$ cd socket.io
$ npm install
$ cd examples/chat
$ npm install
$ npm install --save socketio-wildcard
$ npm start

install

$ pkg .

BlacklistedEvents
"connect"
"connect_error"
"connect_timeout"
"disconnect"
"error"
"reconnect"
"reconnect_attempt"
"reconnect_failed"
"reconnect_error"
"reconnecting"